<UserControl x:Class="ChaosAssetManager.Controls.EquipmentEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="563"
             d:DesignWidth="875"
             Background="{DynamicResource MaterialDesignPaper}"
             TextElement.FontWeight="Medium"
             TextElement.FontSize="14"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             FontFamily="{materialDesign:MaterialDesignFont}"
             DataContext="{Binding RelativeSource={RelativeSource Self}}"
             Loaded="EquipmentEditorControl_OnLoaded">
    <Grid>
        <!-- Archive Path Not Configured Message -->
        <TextBlock x:Name="NotConfiguredMessage"
                   Text="Please configure the Archives Directory in Settings (gear icon)"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontSize="16"
                   Visibility="Collapsed" />

        <!-- Main Content -->
        <Grid x:Name="MainContent">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Left Panel: Selection Controls -->
            <Border Grid.Column="0"
                    BorderBrush="{DynamicResource MaterialDesignDivider}"
                    BorderThickness="0,0,1,0">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!-- Gender Toggle -->
                    <StackPanel Grid.Row="0" Margin="0,0,0,15">
                        <TextBlock Text="Gender"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,5" />
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <RadioButton x:Name="MaleRadio"
                                         Content="Male"
                                         IsChecked="True"
                                         Margin="0,0,10,0"
                                         Checked="Gender_OnChecked" />
                            <RadioButton x:Name="FemaleRadio"
                                         Content="Female"
                                         Checked="Gender_OnChecked" />
                        </StackPanel>
                    </StackPanel>

                    <!-- Equipment Type Dropdown -->
                    <StackPanel Grid.Row="1" Margin="0,0,0,15">
                        <TextBlock Text="Equipment Type" Margin="0,0,0,5" />
                        <ComboBox x:Name="EquipmentTypeCmb"
                                  HorizontalAlignment="Stretch"
                                  SelectionChanged="EquipmentType_OnSelectionChanged"
                                  Style="{StaticResource MaterialDesignComboBox}" />
                    </StackPanel>

                    <!-- Entry List -->
                    <StackPanel Grid.Row="2">
                        <TextBlock Text="Entries" Margin="0,0,0,5" />
                        <ListView x:Name="EntryListView"
                                  HorizontalAlignment="Stretch"
                                  MaxHeight="400"
                                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                                  SelectionChanged="Entry_OnSelectionChanged" />
                    </StackPanel>

                    <!-- Save Button -->
                    <Button x:Name="SaveBtn"
                            Grid.Row="3"
                            Content="Save to Archive"
                            Margin="0,15,0,0"
                            HorizontalAlignment="Stretch"
                            Click="Save_OnClick"
                            Style="{StaticResource MaterialDesignFlatSecondaryMidBgButton}" />
                </Grid>
            </Border>

            <!-- Right Panel: Editor -->
            <ContentControl x:Name="ContentPanel"
                            Grid.Column="1" />
        </Grid>

        <materialDesign:Snackbar x:Name="Snackbar"
                                 HorizontalAlignment="Center"
                                 VerticalAlignment="Bottom"
                                 MessageQueue="{materialDesign:MessageQueue}" />
    </Grid>
</UserControl>
