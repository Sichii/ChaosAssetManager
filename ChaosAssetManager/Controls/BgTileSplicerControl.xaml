<UserControl x:Class="ChaosAssetManager.Controls.BgTileSplicerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:previewControls="clr-namespace:ChaosAssetManager.Controls.PreviewControls"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="800"
             Background="{DynamicResource MaterialDesignPaper}"
             TextElement.FontWeight="Medium"
             TextElement.FontSize="14"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             FontFamily="{materialDesign:MaterialDesignFont}"
             DataContext="{Binding RelativeSource={RelativeSource Self}}"
             Loaded="BgTileSplicerControl_OnLoaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Info Message -->
        <TextBlock Grid.Row="0"
                   Text="Load a PNG image and align the isometric grid over it.&#10;Left-drag to pan, scroll to zoom, right-drag to move the grid."
                   HorizontalAlignment="Center"
                   TextWrapping="Wrap"
                   TextAlignment="Center"
                   MaxWidth="700"
                   Margin="20,15,20,5"
                   FontSize="13"
                   Opacity="0.7" />


        <!-- Controls Row -->
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="20,10">
            <Button x:Name="BrowseBtn"
                    Content="Load Image"
                    Click="BrowseBtn_OnClick"
                    Margin="0,0,10,0"
                    Style="{StaticResource MaterialDesignFlatSecondaryMidBgButton}" />
            <Button x:Name="SendToImportBtn"
                    Content="Send to Tile Import"
                    Click="SendToImportBtn_OnClick"
                    IsEnabled="False"
                    Style="{StaticResource MaterialDesignFlatSecondaryMidBgButton}" />
        </StackPanel>

        <!-- Preview Area -->
        <Border Grid.Row="2"
                Margin="20,10"
                BorderBrush="{DynamicResource MaterialDesignDivider}"
                BorderThickness="1"
                CornerRadius="4"
                Background="#1A1A1A">
            <previewControls:SKGLElementPlus x:Name="Preview"
                                              Paint="Preview_OnPaint"
                                              MouseDown="Preview_OnMouseDown"
                                              MouseMove="Preview_OnMouseMove"
                                              MouseUp="Preview_OnMouseUp" />
        </Border>

        <!-- Snackbar -->
        <materialDesign:Snackbar x:Name="Snackbar"
                                 Grid.Row="3"
                                 HorizontalAlignment="Center"
                                 VerticalAlignment="Bottom"
                                 MessageQueue="{materialDesign:MessageQueue}" />
    </Grid>
</UserControl>