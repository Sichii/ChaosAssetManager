<UserControl x:Class="ChaosAssetManager.Controls.IconImportControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="700"
             Background="{DynamicResource MaterialDesignPaper}"
             TextElement.FontWeight="Medium"
             TextElement.FontSize="14"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             FontFamily="{materialDesign:MaterialDesignFont}"
             DataContext="{Binding RelativeSource={RelativeSource Self}}"
             Loaded="IconImportControl_OnLoaded">
    <Grid>
        <TextBlock x:Name="NotConfiguredMessage"
                   Text="Please configure the Archives Directory in Settings (gear icon)"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontSize="16"
                   Visibility="Collapsed" />

        <TextBlock x:Name="InfoMessage"
                   Text="Select a folder containing PNG images. Images are scaled to 31x31, and the learnable and locked variants are generated from the icons already in the archive."
                   HorizontalAlignment="Center"
                   VerticalAlignment="Top"
                   TextWrapping="Wrap"
                   TextAlignment="Center"
                   MaxWidth="500"
                   Margin="20"
                   FontSize="13"
                   Opacity="0.7" />

        <Grid x:Name="MainContent"
              Margin="20"
              VerticalAlignment="Center"
              HorizontalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="300" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0"
                       Grid.Column="0"
                       Text="Input Path:"
                       VerticalAlignment="Center"
                       Margin="0,0,10,10" />
            <TextBox x:Name="InputPathTxt"
                     Grid.Row="0"
                     Grid.Column="1"
                     Margin="0,0,10,10"
                     Style="{StaticResource MaterialDesignTextBox}"
                     ToolTip="Path to folder containing PNG icon images" />
            <Button x:Name="BrowseInputBtn"
                    Grid.Row="0"
                    Grid.Column="2"
                    Content="Browse"
                    Margin="0,0,0,10"
                    Click="BrowseInputBtn_OnClick"
                    Style="{StaticResource MaterialDesignFlatSecondaryMidBgButton}" />

            <StackPanel Grid.Row="1"
                        Grid.Column="0"
                        Grid.ColumnSpan="3"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        Margin="0,10,0,10">
                <RadioButton x:Name="SkillRadio"
                             Content="Skill Icons"
                             GroupName="IconFamily"
                             IsChecked="True"
                             Margin="0,0,20,0" />
                <RadioButton x:Name="SpellRadio"
                             Content="Spell Icons"
                             GroupName="IconFamily" />
            </StackPanel>

            <Button x:Name="ImportBtn"
                    Grid.Row="2"
                    Grid.Column="0"
                    Grid.ColumnSpan="3"
                    Content="Import Icons"
                    Margin="0,20,0,0"
                    HorizontalAlignment="Center"
                    Click="ImportBtn_OnClick"
                    Style="{StaticResource MaterialDesignFlatSecondaryMidBgButton}" />
        </Grid>

        <TextBlock x:Name="BottomInfoMessage"
                   Text="Icons are written to setoa.dat, filling deleted slots before appending to the end. All skill and spell icons share gui06.pal, so the palette is rebuilt across every icon sheet on each import."
                   HorizontalAlignment="Center"
                   VerticalAlignment="Bottom"
                   TextWrapping="Wrap"
                   TextAlignment="Center"
                   MaxWidth="500"
                   Margin="20,20,20,40"
                   FontSize="13"
                   Opacity="0.7" />

        <materialDesign:Snackbar x:Name="Snackbar"
                                 HorizontalAlignment="Center"
                                 VerticalAlignment="Bottom"
                                 MessageQueue="{materialDesign:MessageQueue}" />
    </Grid>
</UserControl>
